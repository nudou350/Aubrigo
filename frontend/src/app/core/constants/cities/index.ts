// Re-export everything from city-config for easy imports
export * from './city-config';

// Note: Individual city files (portugal-cities.ts, brazil-cities.ts, etc.)
// are NOT exported here to enable lazy loading via dynamic imports
